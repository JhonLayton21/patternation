/**
 * PHASE 4: Presets
 * Public API para el sistema de presets
 */

export { DEFAULT_PRESETS, NOTEBOOK_GRID, DOT_JOURNAL, ISOMETRIC_PAPER, MINIMAL_GRID } from './defaultPresets';
export type { PresetConfig, PatternState, PresetStore } from './PresetConfig';
